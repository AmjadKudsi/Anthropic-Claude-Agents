// create a well-documented TypeScript function that follows the patterns shown in the lesson, then test it to make sure everything works correctly

// TODO: Import the sumNumbers function from functions
import { sumNumbers } from "./functions";

// TODO: Call sumNumbers with values 8 and 12, store the result in a variable
const result = sumNumbers(8, 12);

// TODO: Print the result
console.log(`sumNumbers(8, 12) = ${result}`);