// create a well-documented TypeScript function that follows the patterns shown in the lesson, then test it to make sure everything works correctly

// TODO: Write the sumNumbers function with proper type annotations, JSDoc comment, and implementation

/** 
 * Sum two numbers and return the result.
 * 
 * @param a - First number to add
 * @param b - Second number to add
 * @returns The sum a and b
 */

export function sumNumbers(a: number, b: number): number {
    return a + b;
}