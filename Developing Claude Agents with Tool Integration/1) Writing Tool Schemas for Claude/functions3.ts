// transform your single-tool setup into a multi-tool system by adding a multiplyNumbers function alongside your existing sumNumbers function

/**
 * Sum two numbers and return the result.
 * 
 * @param a - First number to add
 * @param b - Second number to add
 * @returns The sum of a and b
 */
export function sumNumbers(a: number, b: number): number {
    return a + b;
}

// TODO: Add the multiplyNumbers function with the same documentation standards as sumNumbers
export function multiplyNumbers(a: number, b: number): number {
    return a * b;
}